// Public API placeholder. Task 9 fills this in with the real barrel
// (runGame, factories, save helpers, ScriptIO, types). Until then this stub
// keeps the package's declared `exports` field resolvable so `pnpm --filter
// @open-adventure/core build` produces a real dist/index.js.
export {};
